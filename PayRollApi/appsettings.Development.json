{
  "Jwt": {
    // Local demo only — production keeps the 15 min / 7 day defaults in appsettings.json.
    "AccessTokenMinutes": 720,
    "RefreshTokenDays": 30
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
